<Window x:Class="Management_System_WPF.Views.EditSaleWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Edit Sale"
        Height="500" Width="380"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize">

    <Window.Resources>
        <SolidColorBrush x:Key="PrimaryColor" Color="#009688"/>
        <SolidColorBrush x:Key="PrimaryHover" Color="#00796B"/>
        <SolidColorBrush x:Key="TextDark" Color="#37474F"/>
        <SolidColorBrush x:Key="TextLight" Color="#78909C"/>
        <SolidColorBrush x:Key="ReadOnlyBg" Color="#F5F5F5"/>

        <Style TargetType="TextBox" x:Key="ModernInput">
            <Setter Property="Height" Value="40"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="BorderBrush" Value="#CFD8DC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="5">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Button" x:Key="PrimaryButton">
            <Setter Property="Background" Value="{StaticResource PrimaryColor}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource PrimaryHover}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="Button" x:Key="SecondaryButton">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="{StaticResource TextLight}"/>
            <Setter Property="BorderBrush" Value="#CFD8DC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#F5F5F5"/>
                    <Setter Property="Foreground" Value="{StaticResource TextDark}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Border Background="White" 
            CornerRadius="15" 
            BorderBrush="{StaticResource PrimaryColor}" 
            BorderThickness="1"
            Margin="10">

       

        <Grid Margin="25">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="0,0,0,20">
                <TextBlock Text="Edit Sale Details" 
                           FontSize="20" 
                           FontWeight="Bold" 
                           Foreground="{StaticResource TextDark}"
                           VerticalAlignment="Center"/>

                <Button Content="✕" 
                        HorizontalAlignment="Right"
                        Background="Transparent"
                        BorderThickness="0"
                        Foreground="{StaticResource TextLight}"
                        FontSize="16"
                        Cursor="Hand"
                        IsCancel="True"/>
            </Grid>

            <StackPanel Grid.Row="1">

                <TextBlock Text="Customer Name" Foreground="{StaticResource TextLight}" Margin="0,0,0,5"/>
                <TextBox x:Name="txtBuyer" 
                         IsReadOnly="True" 
                         Style="{StaticResource ModernInput}"
                         Background="{StaticResource ReadOnlyBg}"
                         Foreground="{StaticResource TextLight}"
                         Margin="0,0,0,15"
                         Focusable="False"/>

                <TextBlock Text="Item / Article" Foreground="{StaticResource TextLight}" Margin="0,0,0,5"/>
                <TextBox x:Name="txtItem" 
                         IsReadOnly="True" 
                         Style="{StaticResource ModernInput}"
                         Background="{StaticResource ReadOnlyBg}"
                         Foreground="{StaticResource TextLight}"
                         Margin="0,0,0,15"
                         Focusable="False"/>
                <TextBlock Text="Sale Date (Edit)"
           Foreground="{StaticResource PrimaryColor}"
           FontWeight="SemiBold"
           Margin="0,0,0,5"/>

                <DatePicker x:Name="dpSaleDate"
            Height="40"
            FontSize="14"
            SelectedDateFormat="Short"
            Margin="0,0,0,15"/>


                <TextBlock Text="Quantity (Edit)" Foreground="{StaticResource PrimaryColor}" FontWeight="SemiBold" Margin="0,0,0,5"/>
                <TextBox x:Name="txtQty" 
                         Style="{StaticResource ModernInput}"
                         FontWeight="Bold"
                         Foreground="{StaticResource TextDark}"
                         BorderBrush="{StaticResource PrimaryColor}"/>
            </StackPanel>

            <Grid Grid.Row="2" Margin="0,25,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="15"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0" 
                        Content="Cancel" 
                        Style="{StaticResource SecondaryButton}" 
                        Height="40"
                        IsCancel="True"/>

                <Button Grid.Column="1" Grid.ColumnSpan="2"
                        Content="Save Changes" 
                        Style="{StaticResource PrimaryButton}" 
                        Height="40"
                        Click="Save_Click"
                        IsDefault="True"/>
            </Grid>

        </Grid>
    </Border>
</Window>