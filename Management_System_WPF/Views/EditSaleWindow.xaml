<Window x:Class="Management_System_WPF.Views.EditSaleWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Edit Sale"
        Height="550" Width="420"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize">

    <Window.Resources>
        <SolidColorBrush x:Key="PrimaryColor" Color="#009688"/>
        <SolidColorBrush x:Key="PrimaryHover" Color="#00796B"/>
        <SolidColorBrush x:Key="SecondaryBg" Color="#F7F9FA"/>
        <SolidColorBrush x:Key="TextDark" Color="Black"/>
        <SolidColorBrush x:Key="TextLight" Color=" #454545"/>
        <SolidColorBrush x:Key="BorderColor" Color="#E0E0E0"/>

        <Style TargetType="TextBox" x:Key="InputStyle">
            <Setter Property="Height" Value="45"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="10">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Button" x:Key="PrimaryButtonStyle">
            <Setter Property="Background" Value="{StaticResource PrimaryColor}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="10">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource PrimaryHover}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="Button" x:Key="CancelButtonStyle">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TextLight}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="10">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Foreground" Value="#D32F2F"/>
                    <Setter Property="Background" Value="#FFEBEE"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Border Background="White"
            CornerRadius="12"
            Margin="15">

        <Border.Effect>
            <DropShadowEffect BlurRadius="25" ShadowDepth="5" Opacity="0.25" Color="#303030"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="25,20">
                <TextBlock Text="Edit Transaction" 
                           FontSize="20" FontWeight="Bold" 
                           Foreground="{StaticResource TextDark}" 
                           VerticalAlignment="Center"/>

                <Button Content="✕" 
                        HorizontalAlignment="Right" VerticalAlignment="Top"
                        FontSize="18" Foreground="{StaticResource TextLight}"
                        Background="Transparent" BorderThickness="0"
                        Cursor="Hand" IsCancel="True"/>
            </Grid>

            <Border Grid.Row="1" Background="{StaticResource SecondaryBg}" 
                    Margin="25,0,25,20" Padding="15" CornerRadius="8"
                    BorderBrush="{StaticResource BorderColor}" BorderThickness="1">
                <StackPanel>
                    <Grid Margin="0,0,0,10">
                        <StackPanel>
                            <TextBlock Text="BUYER" FontSize="11" FontWeight="Bold" Foreground="{StaticResource TextLight}"/>
                            <TextBox x:Name="txtBuyer" Text="John Doe" 
                                     Background="Transparent" BorderThickness="0" 
                                     FontWeight="SemiBold" FontSize="15" Foreground="{StaticResource TextDark}"
                                     IsReadOnly="True" Focusable="False" Padding="0,2,0,0"/>
                        </StackPanel>
                    </Grid>
                    <Separator Background="{StaticResource BorderColor}" Margin="0,0,0,10"/>
                    <Grid>
                        <StackPanel>
                            <TextBlock Text="ARTICLE / ITEM" FontSize="11" FontWeight="Bold" Foreground="{StaticResource TextLight}"/>
                            <TextBox x:Name="txtItem" Text="Cotton Shirt XL" 
                                     Background="Transparent" BorderThickness="0" 
                                     FontWeight="SemiBold" FontSize="15" Foreground="{StaticResource TextDark}"
                                     IsReadOnly="True" Focusable="False" Padding="0,2,0,0"/>
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </Border>

            <StackPanel Grid.Row="2" Margin="25,0">

                <TextBlock Text="New Sale Date" Foreground="{StaticResource PrimaryColor}" FontWeight="SemiBold" Margin="0,0,0,6"/>
                <Grid Margin="0,0,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Border Background="{StaticResource SecondaryBg}" Width="45" Height="45" CornerRadius="10,0,0,10" BorderBrush="{StaticResource BorderColor}" BorderThickness="1,1,0,1">
                        <Path Data="M19,19H5V8H19M16,1V3H8V1H6V3H5C3.89,3 3,3.89 3,5V19A2,2 0 0,0 5,21H19A2,2 0 0,0 21,19V5C21,3.89 20.1,3 19,3H18V1M17,12H12V17H17V12Z" 
                              Fill="{StaticResource TextLight}" Width="18" Height="18" Stretch="Uniform"/>
                    </Border>

                    <Border Background="#80F5F5F5"  Grid.Column="1" BorderBrush="#D0D0D0" BorderThickness="0,1,1,1" CornerRadius="0,10,10,0" Height="45">

                        <DatePicker x:Name="dpSaleDate" Grid.Column="1"                 SelectedDateFormat="Short" 
                 FontSize="15"  BorderThickness="0"
                  VerticalContentAlignment="Center" Height="45"
                                   Background="Transparent" >

                            <DatePicker.Resources>
                                <Style TargetType="DatePickerTextBox">
                                    <Setter Property="Background" Value="Transparent"/>
                                    <Setter Property="BorderThickness" Value="0"/>
                                    <Setter Property="Padding" Value="5,0"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="DatePickerTextBox">
                                                <Grid>
                                                    <ScrollViewer x:Name="PART_ContentHost"/>
                                                    <TextBlock Text="Select Date" 
    Foreground="#999" 
    IsHitTestVisible="False"
    Margin="5,0,0,0"
    VerticalAlignment="Center">
                                                        <TextBlock.Style>
                                                            <Style TargetType="TextBlock">
                                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding Text, RelativeSource={RelativeSource AncestorType=DatePickerTextBox}}" Value="">
                                                                        <Setter Property="Visibility" Value="Visible"/>
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </TextBlock.Style>
                                                    </TextBlock>
                                                </Grid>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </DatePicker.Resources>
                        </DatePicker>
                    </Border>
                </Grid>

                <TextBlock Text="Updated Quantity" Foreground="{StaticResource PrimaryColor}" FontWeight="SemiBold" Margin="0,0,0,6"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Border Background="{StaticResource SecondaryBg}" Width="45" Height="45" CornerRadius="10,0,0,10" BorderBrush="{StaticResource BorderColor}" BorderThickness="1,1,0,1">
                        <Path Data="M11,13.5V21.5H9V13.5H11M12,2L17.5,11H6.5L12,2M12,5.86L10.08,9H13.92L12,5.86Z" 
                              Fill="{StaticResource TextLight}" Width="18" Height="18" Stretch="Uniform"/>
                    </Border>

                    <TextBox x:Name="txtQty" Grid.Column="1" Style="{StaticResource InputStyle}" 
                             FontWeight="Bold" Foreground="{StaticResource TextDark}">
                        <TextBox.Template>
                            <ControlTemplate TargetType="TextBox">
                                <Border Background="White" BorderBrush="{StaticResource BorderColor}" BorderThickness="0,1,1,1" CornerRadius="0,10,10,0">
                                    <ScrollViewer x:Name="PART_ContentHost"/>
                                </Border>
                            </ControlTemplate>
                        </TextBox.Template>
                    </TextBox>
                </Grid>
            </StackPanel>

            <Grid Grid.Row="3" Margin="25,30,25,25">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="15"/>
                    <ColumnDefinition Width="1.5*"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0" Content="Cancel" Style="{StaticResource CancelButtonStyle}" Height="45" IsCancel="True"/>
                <Button Grid.Column="2" Content="Save Changes" Style="{StaticResource PrimaryButtonStyle}" Height="45" Click="Save_Click" IsDefault="True"/>
            </Grid>

        </Grid>
    </Border>
</Window>