<Window x:Class="Management_System_WPF.Views.PrintPreviewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Print Preview" 
        Height="800" Width="1000"
        WindowState="Maximized"
        WindowStartupLocation="CenterScreen">

    <Grid Background="#F0F0F0">
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="White" BorderBrush="Gray" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="20,0">

                <Button Content="📄 Save PDF" 
                        Height="35" Width="100" 
                        Background="#D32F2F" Foreground="White" FontWeight="Bold"
                        Cursor="Hand" Click="PDF_Click" Margin="0,0,10,0">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="5"/>
                        </Style>
                    </Button.Resources>
                </Button>

                <Button Content="🖨 Print" 
                        Height="35" Width="100" 
                        Background="#009688" Foreground="White" FontWeight="Bold"
                        Cursor="Hand" Click="Print_Click">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="5"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </StackPanel>
        </Border>

        <DocumentViewer x:Name="docViewer" Grid.Row="1">
            <DocumentViewer.Resources>
                <Style TargetType="ContentControl" x:Key="HiddenToolbar">
                    <Setter Property="Visibility" Value="Collapsed"/>
                </Style>
            </DocumentViewer.Resources>
        </DocumentViewer>
    </Grid>
</Window>