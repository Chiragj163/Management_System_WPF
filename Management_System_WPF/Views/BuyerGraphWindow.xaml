<Window x:Class="Management_System_WPF.Views.BuyerGraphWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
        Title="Sales Analysis" 
        Height="750" MinWidth="800"  MaxWidth="1900"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize">

    <Window.Resources>
        <SolidColorBrush x:Key="PrimaryColor" Color="#009688"/>
        <SolidColorBrush x:Key="TextDark" Color="#37474F"/>
        <SolidColorBrush x:Key="TextLight" Color="#78909C"/>
        <SolidColorBrush x:Key="BorderColor" Color="#E0E0E0"/>

        <Style TargetType="Button" x:Key="CloseBtnStyle">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="{StaticResource TextLight}"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="50">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#FFEBEE"/>
                    <Setter Property="Foreground" Value="#D32F2F"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Border Background="White" 
            CornerRadius="12" 
            Margin="15" 
            BorderBrush="{StaticResource PrimaryColor}" 
            BorderThickness="1">

        <Border.Effect>
            <DropShadowEffect BlurRadius="25" ShadowDepth="5" Opacity="0.25" Color="Black"/>
        </Border.Effect>

        <Grid Margin="25">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="0,0,0,20">
                <StackPanel HorizontalAlignment="Center">
                   

                    <TextBlock x:Name="txtTitle" 
                               Text="Sales Overview" 
                               FontSize="24" 
                               FontWeight="Bold" 
                               Foreground="{StaticResource TextDark}" 
                               TextAlignment="Center" 
                               Margin="0,5,0,0"/>
                </StackPanel>

                <Button Content="✕" 
                        Style="{StaticResource CloseBtnStyle}" 
                        HorizontalAlignment="Right" 
                        VerticalAlignment="Top" 
                        Width="35" Height="35" 
                        Click="Close_Click" 
                        IsCancel="True"/>
            </Grid>

            <Border Grid.Row="1" 
                    Background="White" 
                    BorderBrush="{StaticResource BorderColor}" 
                    BorderThickness="1" 
                    CornerRadius="8" 
                    Padding="20">
                <ScrollViewer HorizontalScrollBarVisibility="Auto"
              VerticalScrollBarVisibility="Disabled">
                    <lvc:CartesianChart x:Name="SalesChart" LegendLocation="Bottom" Hoverable="True">
                    <lvc:CartesianChart.AxisX>
                        <lvc:Axis Title="Buyers" Labels="{Binding BuyerLabels}" Foreground="{StaticResource TextDark}">
                            <lvc:Axis.Separator>
                                <lvc:Separator Step="1" StrokeThickness="0"/>
                            </lvc:Axis.Separator>
                        </lvc:Axis>
                    </lvc:CartesianChart.AxisX>

                    <lvc:CartesianChart.AxisY>
                        <lvc:Axis Title="Amount (₹)" LabelFormatter="{Binding CurrencyFormatter}" Foreground="{StaticResource TextDark}">
                            <lvc:Axis.Separator>
                                <lvc:Separator Stroke="#F0F0F0" StrokeThickness="1" StrokeDashArray="2"/>
                            </lvc:Axis.Separator>
                        </lvc:Axis>
                    </lvc:CartesianChart.AxisY>

                    <lvc:CartesianChart.DataTooltip>
                        <lvc:DefaultTooltip SelectionMode="SharedYValues" 
                                            Background="{StaticResource TextDark}" 
                                            Foreground="White" 
                                            BulletSize="10"/>
                    </lvc:CartesianChart.DataTooltip>
                </lvc:CartesianChart>
                </ScrollViewer>
            </Border>
        </Grid>
    </Border>
</Window>