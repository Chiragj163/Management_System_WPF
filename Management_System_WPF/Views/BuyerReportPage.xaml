<Page x:Class="Management_System_WPF.Views.BuyerReportPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="Transparent">

    <Grid x:Name="MainContainer" Margin="40">


        <Border Background="#F8FFFFFF"
                CornerRadius="20"
                Padding="30"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch">

            <StackPanel>

                <!-- Back Button -->
                <Button Content="⟵ Back"
                        Width="120"
                        Height="38"
                        Background="#3CB4A7"
                        Foreground="White"
                        BorderThickness="0"
                        Cursor="Hand"
                        HorizontalAlignment="Left"
                        Click="Back_Click"/>
                <Button Content="Previous Month Sales"
        Width="200"
        Height="40"
        Background="#3CB4A7"
        Foreground="White"
        Margin="0,10"
        HorizontalAlignment="Left"               
        Click="PrevMonth_Click"/>

                <TextBlock x:Name="txtBuyerName"
           FontSize="26"
           FontWeight="Bold"
           HorizontalAlignment="Center"
           Margin="0,10"/>


                <!-- Title -->
                <TextBlock Text="Buyer Sale History"
                           FontSize="32"
                           FontWeight="Bold"
                           Foreground="Black"
                           HorizontalAlignment="Center"
                           Margin="0,10"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10">
                    

                    <Button Content="🖨 Print"
            Width="120"
            Height="40"
            Background="#3CB4A7"
            Foreground="White"
            Click="Print_Click"/>
                </StackPanel>


                <!-- === STYLED DATAGRID === -->
                <DataGrid x:Name="dgBuyerReport"
          AutoGenerateColumns="False"
          CanUserAddRows="False"
          HeadersVisibility="Column"
          RowHeight="40"
          FontSize="18"
          AlternationCount="2"
          Background="White"
          BorderBrush="#DDD"
          BorderThickness="1"
          Margin="0,20,0,0">

                    <!-- HEADER STYLE -->
                    <DataGrid.ColumnHeaderStyle>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="#3CB4A7"/>
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="FontSize" Value="18"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                            <Setter Property="Height" Value="45"/>
                        </Style>
                    </DataGrid.ColumnHeaderStyle>

                    <!-- ROW STYLE -->
                    <DataGrid.RowStyle>
                        <Style TargetType="DataGridRow">
                            <Setter Property="FontSize" Value="18"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>

                            <Style.Triggers>

                                <!-- Alternate Row Coloring -->
                                <Trigger Property="ItemsControl.AlternationIndex" Value="1">
                                    <Setter Property="Background" Value="#F3F9F9"/>
                                </Trigger>

                                <!-- Hover -->
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#D7EFEA"/>
                                </Trigger>

                            </Style.Triggers>
                        </Style>
                    </DataGrid.RowStyle>

                    <!-- COLUMNS -->
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Date" Binding="{Binding SaleDate}" Width="*" />
                        <DataGridTextColumn Header="Item" Binding="{Binding ItemName}" Width="1.5*" />
                        <DataGridTextColumn Header="Qty" Binding="{Binding Quantity}" Width="80" />
                        <DataGridTextColumn Header="Price" Binding="{Binding Price}" Width="120" />
                        <DataGridTextColumn Header="Total" Binding="{Binding Total}" Width="120" />
                    </DataGrid.Columns>

                </DataGrid>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10">
                    <TextBlock Text="Grand Total: " FontSize="20" FontWeight="Bold"/>
                    <TextBlock x:Name="txtGrandTotal" FontSize="20" FontWeight="Bold" Foreground="#3CB4A7"/>
                </StackPanel>



            </StackPanel>
        </Border>
    </Grid>
</Page>
