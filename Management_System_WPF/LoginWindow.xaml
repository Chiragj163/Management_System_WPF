<Window x:Class="Management_System_WPF.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login" Height="500" Width="750"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        ResizeMode="NoResize"
        Background="Transparent"
        AllowsTransparency="True"
        KeyDown="Window_KeyDown">

    <Window.Resources>
        <SolidColorBrush x:Key="PrimaryColor" Color="#009688"/>
        <SolidColorBrush x:Key="PrimaryHover" Color="#00796B"/>
        <SolidColorBrush x:Key="TextDark" Color="#37474F"/>
        <SolidColorBrush x:Key="TextLight" Color="#90A4AE"/>
        <SolidColorBrush x:Key="InputBg" Color="#F7F9FA"/>

        <Style x:Key="CloseBtnStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="50">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#FFEBEE"/>
                    <Setter Property="Foreground" Value="#D32F2F"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Border CornerRadius="20" Background="White" Margin="20">
        <Border.Effect>
            <DropShadowEffect BlurRadius="30" ShadowDepth="5" Opacity="0.3" Color="Black"/>
        </Border.Effect>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" CornerRadius="20,0,0,20" ClipToBounds="True" Background="#E0F2F1">
                <Grid>
                    <Border>
                        <Border.Background>
                            <ImageBrush x:Name="LoginBg" ImageSource="/Assets/bg.jpg"  Stretch="UniformToFill" Opacity="0.9"/>
                        </Border.Background>
                    </Border>

                    <Border Background="#40009688"/>

                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                        <TextBlock Text="Welcome" FontSize="32" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center"/>
                        <TextBlock Text="Management System" FontSize="16" Foreground="White" Opacity="0.9" HorizontalAlignment="Center" Margin="0,5,0,0"/>
                    </StackPanel>
                </Grid>
            </Border>

            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Button Grid.Row="0" Content="✕" Click="Exit_Click"
                        Style="{StaticResource CloseBtnStyle}"
                        Foreground="#90A4AE" FontSize="16" FontWeight="Bold"
                        HorizontalAlignment="Right" Width="40" Height="40" Margin="0,10,10,0"/>

                <StackPanel Grid.Row="1" VerticalAlignment="Center" Margin="50,0,50,30">

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,30">
                        <Border Width="50" Height="50" CornerRadius="25" Background="{StaticResource InputBg}" Margin="0,0,10,0">
                            <Path Data="M12,4A4,4 0 0,1 16,8A4,4 0 0,1 12,12A4,4 0 0,1 8,8A4,4 0 0,1 12,4M12,14C16.42,14 20,15.79 20,18V20H4V18C4,15.79 7.58,14 12,14Z"
                                  Fill="{StaticResource PrimaryColor}" Stretch="Uniform" Width="24" Height="24" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="Login" FontSize="24" FontWeight="Bold" Foreground="{StaticResource TextDark}"/>
                            <TextBlock Text="Sign in to your account" FontSize="12" Foreground="{StaticResource TextLight}"/>
                        </StackPanel>
                    </StackPanel>

                    <TextBlock Text="Username" Foreground="{StaticResource TextLight}" FontWeight="SemiBold" Margin="5,0,0,5"/>
                    <Border Background="{StaticResource InputBg}" CornerRadius="8" Height="45" Padding="10,0">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Path Data="M12,4A4,4 0 0,1 16,8A4,4 0 0,1 12,12A4,4 0 0,1 8,8A4,4 0 0,1 12,4M12,14C16.42,14 20,15.79 20,18V20H4V18C4,15.79 7.58,14 12,14Z" Fill="{StaticResource TextLight}" Width="14" Stretch="Uniform" VerticalAlignment="Center" Margin="0,0,10,0"/>
                            <TextBox x:Name="txtUser" Grid.Column="1" Background="Transparent" BorderThickness="0" VerticalContentAlignment="Center" FontSize="14" Foreground="{StaticResource TextDark}"/>
                        </Grid>
                    </Border>

                    <TextBlock Text="Password" Foreground="{StaticResource TextLight}" FontWeight="SemiBold" Margin="5,20,0,5"/>
                    <Border Background="{StaticResource InputBg}" CornerRadius="8" Height="45" Padding="10,0">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Path Data="M12,17A2,2 0 0,0 14,15C14,13.89 13.1,13 12,13A2,2 0 0,0 10,15A2,2 0 0,0 12,17M18,8A2,2 0 0,1 20,10V20A2,2 0 0,1 18,22H6A2,2 0 0,1 4,20V10C4,8.89 4.9,8 6,8H7V6A5,5 0 0,1 12,1A5,5 0 0,1 17,6V8H18M12,3A3,3 0 0,0 9,6V8H15V6A3,3 0 0,0 12,3Z"
                                  Fill="{StaticResource TextLight}" Width="14" Stretch="Uniform" VerticalAlignment="Center" Margin="0,0,10,0"/>
                            <PasswordBox x:Name="txtPass" Grid.Column="1" Background="Transparent" BorderThickness="0" VerticalContentAlignment="Center" FontSize="14" Foreground="{StaticResource TextDark}"/>
                        </Grid>
                    </Border>

                    <Button Content="Change Password?" 
                            Click="Reset_Click"
                            Background="Transparent" BorderThickness="0" 
                            Foreground="{StaticResource PrimaryColor}" 
                            HorizontalAlignment="Right" Margin="0,5,0,0" 
                            FontSize="12" Cursor="Hand"/>

                    <Button Content="LOGIN" IsDefault="True" Height="50" Margin="0,30,0,0"
                            Background="{StaticResource PrimaryColor}" Foreground="White"
                            FontSize="16" FontWeight="Bold" Cursor="Hand"
                            Click="Login_Click" BorderThickness="0">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="bdr" Background="{TemplateBinding Background}" CornerRadius="25">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="bdr" Property="Background" Value="{StaticResource PrimaryHover}"/>
                                        <Setter TargetName="bdr" Property="Effect">
                                            <Setter.Value>
                                                <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.3" Color="#009688"/>
                                            </Setter.Value>
                                        </Setter>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>

                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</Window>